import React from 'react';
import { Container } from 'react-bootstrap';
import ListTaxis from './components/ListTaxis';

export default ()=>{
    return (
      <Container>
          <ListTaxis/>
      </Container>
      );
};